class Durations {
  static const Duration PAGE_TRANSITION_DURATION = Duration(milliseconds: 700);
  static const Duration LOADING_DURATION = Duration(milliseconds: 500);
  static const Duration REQUEST_DURATION = Duration(seconds: 1);
}
